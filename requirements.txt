# Runtime dependencies
# =============================================================================
# Everything needed to run the pipeline and the dashboard. This is the file
# Streamlit Community Cloud installs, so it is kept deliberately lean: no
# notebook tooling, no test runner, and nothing that compiles from source.
#
# Development extras (notebooks, tests, the data downloader, the CVXPY solver)
# live in requirements-dev.txt.

# Core scientific stack
numpy>=1.26
pandas>=2.1
scipy>=1.11
scikit-learn>=1.3          # Ledoit-Wolf covariance shrinkage

# Configuration
PyYAML>=6.0

# Static figures produced by `python -m prisk.pipeline`
matplotlib>=3.8
seaborn>=0.13

# Interactive dashboard
streamlit>=1.35
plotly>=5.20
